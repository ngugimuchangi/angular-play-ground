import { MyNgStyleDirective } from './my-ng-style.directive';

describe('MyNgStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new MyNgStyleDirective();
    expect(directive).toBeTruthy();
  });
});
